{
  "name": "steady-catch",
  "version": "0.1.0",
  "type": "module",
  "description": "A bilingual agent skill for controlled steady-catch AI catchphrase energy.",
  "bin": {
    "steady-catch": "./scripts/steady-catch.mjs",
    "steady-catch-rules": "./scripts/generate-agent-rules.mjs"
  },
  "scripts": {
    "generate:rules": "node scripts/steady-catch.mjs init",
    "preview:rules": "node scripts/steady-catch.mjs init --all --dry-run",
    "targets": "node scripts/steady-catch.mjs targets"
  }
}
